import React from 'react';
import { SafeAreaView, View, StyleSheet, StatusBar, FlatList, Text } from 'react-native';

export default function SelectedOrder () {
    return (
        <Text>sadas</Text>
    )
}